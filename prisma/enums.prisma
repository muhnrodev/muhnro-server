enum MFAMethod {
    SMS
    EMAIL
    TOTP
    WEBAUTHN
}

enum AuthEventType {
    ACCOUNT_CREATED
    ACCOUNT_VERIFIED

    LOGIN_SUCCESS
    LOGIN_FAILED
    LOGIN_2FA_REQUIRED
    LOGIN_2FA_SUCCESS
    LOGIN_2FA_FAILED

    LOGOUT
    SESSION_EXPIRED
    TOKEN_REFRESHED

    PASSWORD_CHANGED
    PASSWORD_RESET_REQUESTED
    PASSWORD_RESET_COMPLETED

    ACCOUNT_LOCKED
    ACCOUNT_UNLOCKED
    ACCOUNT_DISABLED
    ACCOUNT_ENABLED

    EMAIL_VERIFICATION_SENT
    EMAIL_VERIFIED
    EMAIL_CHANGED

    MFA_ENABLED
    MFA_DISABLED
    RECOVERY_CODE_USED
}

enum UserRole {
    USER
    ADMIN
    EDITOR
}

enum IdentityProviderCode {
    GOOGLE
    FACEBOOK
    GITHUB
    TWITTER
}

enum ProjectStatus {
    ACTIVE
    INACTIVE
    ARCHIVED
}

enum ArticleStatus {
    DRAFT
    PUBLISHED
    ARCHIVED
}

enum EmploymentType {
    FULL_TIME
    PART_TIME
    CONTRACTOR
    INTERN
}

enum NoteType {
    GENERAL
    TECHNICAL
    MANAGEMENT
    OTHER
}

enum CommentStatus {
    PENDING
    APPROVED
    REJECTED
}

enum ReportStatus {
    DRAFT
    REVIEW
    PUBLISHED
    ARCHIVED
}

enum FieldType {
    STRING
    TEXT
    LONGTEXT
    IDENTITY
    RICHTEXT
    MARKDOWN
    IMAGE
    VIDEO
    AUDIO
    FILE
    DATE
    TIME
    DATETIME
    NUMBER
    BOOLEAN
    COLOR
    URL
    EMAIL
    ARRAY
    OBJECT
    REFERENCE
    REFERENCE_ARRAY
}

enum ReferenceType {
    ARTICLE
    NEWS
    PROJECT
    REPORT
    PODCAST
    PRODUCT
    PARTNER
    PERSON
}